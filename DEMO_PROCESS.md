# CoolTrack — เอกสารสรุป Demo

## 1. วัตถุประสงค์ของ Demo

CoolTrack เป็นตัวอย่างระบบจัดการงานซ่อมและบำรุงรักษาเครื่องปรับอากาศ (AC Service Management) สำหรับร้านแอร์หรือทีมช่างที่ดูแลหลายลูกค้า/หลาย Station

Demo นี้ทำขึ้นเพื่อให้ผู้ใช้งานทดลองลำดับการทำงานและหน้าจอก่อนเริ่มพัฒนาระบบจริง โดยยังไม่ต้องมีฐานข้อมูลหรือบัญชีผู้ใช้จริง

## 2. วิธีเข้าใช้งาน

หลัง deploy บน Vercel ให้ใช้ URL ของ Project เป็นหน้าหลัก

| หน้าจอ | URL | ผู้ใช้หลัก |
|---|---|---|
| Admin Console | `/` | ผู้ดูแลระบบ / เจ้าของกิจการ |
| Station Workspace | `/station.html` | ช่างเทคนิค / เจ้าหน้าที่ประจำ Station |

ทั้งสองหน้ามีปุ่มสลับมุมมองด้านล่างของ sidebar เพื่อให้ทดลองเห็นขอบเขตการใช้งานของแต่ละบทบาท

## 3. บทบาทผู้ใช้งานใน Demo

### Admin

Admin เห็นข้อมูลทุก Station และใช้สำหรับควบคุมงานภาพรวม เช่น

- ดูจำนวนใบงานที่เปิดอยู่ งานรอมอบหมาย และงาน MA ที่ต้องติดตาม
- ดูสถิติการให้บริการและรายการที่ต้องดำเนินการก่อน
- สร้างและติดตามใบงานทุก Station
- เพิ่มข้อมูลเครื่องปรับอากาศและ Station
- สร้างและอนุมัติใบเสนอราคา
- ดูตัวอย่างการกำหนดสิทธิ์ทีมงาน

### Station staff / Technician

เจ้าหน้าที่ Station เห็นเฉพาะข้อมูลของ Station ที่รับผิดชอบ (ใน demo คือ โรงพยาบาลปากน้ำโพ 1) เช่น

- ดูงานของตัวเองในวันนี้
- เริ่มงานและปิดงาน
- ดูเครื่องแอร์เฉพาะ Station
- จำลองการสแกน QR เพื่อเปิดข้อมูลเครื่อง
- บันทึกประวัติการซ่อม/MA

## 4. ฟังก์ชันที่ทดลองกดใช้งานได้

### 4.1 ใบงาน (Work Order)

1. กด `สร้างใบงาน` จากหน้า Admin หรือ `แจ้งงานบริการ` จากหน้า Station
2. ระบุ Station, ลูกค้า, ประเภทงาน, เครื่องแอร์ และรายละเอียด
3. ระบบสร้างรหัสใบงานตัวอย่าง เช่น `JOB-2026080000xx`
4. เปิดใบงานเพื่อดูรายละเอียด
5. กด `เริ่มดำเนินการ` เพื่อเปลี่ยนสถานะเป็นกำลังดำเนินการ
6. กด `ปิดงาน` เพื่อเปลี่ยนสถานะเป็นเสร็จสิ้น

### 4.2 ข้อมูลเครื่องปรับอากาศ (AC Assets)

1. Admin เข้าเมนู `เครื่องปรับอากาศ`
2. ค้นหาจาก Air ID, Serial Number หรือรุ่น
3. กด `เพิ่มเครื่องปรับอากาศ` เพื่อเพิ่มข้อมูลเครื่องตัวอย่าง
4. กด `รายละเอียด` เพื่อดูสถานที่ติดตั้ง รอบ MA และประวัติ service
5. กด `บันทึกงานบริการ` เพื่อเพิ่มประวัติซ่อมหรือ MA

### 4.3 QR Code (แบบจำลอง)

1. กด `สแกน QR` หรือ `สแกน QR เครื่อง`
2. เลือกเครื่องแอร์จากรายการจำลอง
3. ระบบพาไปหน้ารายละเอียดเครื่องและประวัติ service

> ระบบจริงจะเปลี่ยนจากการเลือกในรายการเป็นการเปิดกล้องมือถือเพื่ออ่าน QR Code ที่ติดอยู่บนเครื่องแอร์

### 4.4 ใบเสนอราคา (Quotation)

1. Admin เข้าเมนู `ใบเสนอราคา`
2. กด `สร้างใบเสนอราคา`
3. เลือก Station, ลูกค้า และเพิ่มรายการสินค้า/บริการ
4. ระบบคำนวณยอดรวม, VAT 7% และยอดสุทธิอัตโนมัติ
5. เปิดใบเสนอราคา แล้วกด `อนุมัติใบเสนอราคา`

### 4.5 Station และทีมงาน

- เพิ่ม Station พร้อมชื่อผู้ติดต่อและเบอร์โทรได้
- ดูจำนวนเครื่องและใบงานที่เปิดอยู่ของแต่ละ Station
- ดูตัวอย่างสมาชิกทีมและขอบเขตสิทธิ์ตาม Station

## 5. ข้อมูลใน Demo ทำงานอย่างไร

ข้อมูลที่เพิ่มหรือแก้ไขใน Demo จะเก็บไว้ใน `Local Storage` ของเบราว์เซอร์เครื่องที่กำลังทดลองใช้ เช่น ใบงานใหม่ สถานะงาน ประวัติ service และใบเสนอราคา

ข้อดีคือผู้ทดลองกดใช้งานต่อได้ทันทีโดยไม่ต้องสมัครสมาชิกหรือเชื่อมฐานข้อมูล

ข้อจำกัดคือข้อมูลจะไม่แชร์ข้ามเครื่อง/ข้ามบัญชี และอาจหายได้หากล้างข้อมูลเว็บไซต์ของเบราว์เซอร์

หากต้องการเริ่มข้อมูล Demo ใหม่ ให้ล้าง Site Data ของโดเมน Vercel นั้นในเบราว์เซอร์ แล้วเปิดเว็บใหม่

## 6. โครงสร้างไฟล์ Demo

| ไฟล์ | หน้าที่ |
|---|---|
| `index.html` | จุดเริ่มต้นหน้า Admin Console |
| `station.html` | หน้า Station Workspace สำหรับช่าง/เจ้าหน้าที่ |
| `styles.css` | รูปแบบหน้าจอ responsive สำหรับคอมพิวเตอร์และมือถือ |
| `app.js` | ข้อมูลจำลอง, เมนู, ฟอร์ม, การเปลี่ยนสถานะ และ Local Storage |
| `vercel.json` | การตั้งค่า deploy static site บน Vercel |

## 7. ขั้นตอนอัปเดต Demo

เมื่อแก้ไขไฟล์แล้ว ให้ใช้คำสั่งด้านล่างใน PowerShell:

```powershell
cd C:\xampp\htdocs\cooltrack
git add .
git commit -m "อธิบายสิ่งที่แก้"
git push
```

Vercel จะตรวจพบการ push ไปที่ branch `main` และ deploy เวอร์ชันใหม่อัตโนมัติ

## 8. ข้อจำกัดของ Demo ปัจจุบัน

- ไม่มีระบบ Login หรือ Password จริง
- ไม่มีฐานข้อมูลกลาง ข้อมูลอยู่ในเบราว์เซอร์ของแต่ละคน
- QR เป็นการจำลองการสแกน
- ยังไม่อัปโหลดรูปก่อน/หลังงาน
- ยังไม่สร้าง PDF ใบงานหรือใบเสนอราคา
- สิทธิ์ Admin และ Station เป็นการแยกมุมมองบนหน้าจอ ยังไม่ใช่การป้องกันสิทธิ์จาก server

## 9. แนวทางพัฒนาเป็นระบบจริง

ลำดับที่แนะนำ:

1. ยืนยันหน้าจอและข้อมูลที่ต้องใช้จากการทดลอง Demo
2. สร้างระบบ Login และกำหนดสิทธิ์ Admin/Staff ตาม Station
3. เชื่อมฐานข้อมูลจริงสำหรับ Station, User, AC Asset, Work Order, Service Log และ Quotation
4. ทำ QR Code จริงสำหรับเครื่องแอร์แต่ละตัว
5. เพิ่มอัปโหลดรูปก่อน/หลังงานไปยัง Cloud Storage
6. สร้าง PDF ใบงานและใบเสนอราคา
7. เพิ่มรายงาน Excel/Google Sheets และการแจ้งเตือน MA

สำหรับระบบจริง แนะนำใช้ Vercel เป็นหน้าเว็บและ API, ใช้ฐานข้อมูลจริง (เช่น Supabase หรือ PostgreSQL) และใช้ Google Sheets สำหรับ export/report มากกว่าการใช้เป็นฐานข้อมูลหลัก

---

เอกสารนี้อธิบายขอบเขตของ Demo เวอร์ชันปัจจุบัน เพื่อใช้เก็บ feedback ก่อนเริ่มพัฒนาระบบจริง
